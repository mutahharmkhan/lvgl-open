---
title: lv_text.h
description: API reference for lv_text.h
api:
  functions: [lv_text_get_size]
  enums: [lv_text_flag_t, lv_text_align_t]
---

<RelatedHeaders name="lv_text_private.h" isPrivate={false} />

<ApiSummary functions={1} enums={2} />

## Functions

<ApiMember kind="function" name="lv_text_get_size" file="misc/lv_text.h" line={75} url="https://github.com/lvgl/lvgl/blob/master/src/misc/lv_text.h#L75">

### lv_text_get_size

Get size of a text

```c
void lv_text_get_size(lv_point_t *size_res, const char *text, const lv_font_t *font, int32_t letter_space, int32_t line_space, int32_t max_width, lv_text_flag_t flag)
```

<span className="sr-only">Parameters</span>

| Name | Type | Description |
|------|------|-------------|
| `size_res` | <ApiLink name="lv_point_t" display="lv_point_t *" /> | pointer to a 'point_t' variable to store the result |
| `text` | `const char *` | pointer to a text |
| `font` | <ApiLink name="lv_font_t" display="const lv_font_t *" /> | pointer to font of the text |
| `letter_space` | <ApiLink name="int32_t" /> | letter space of the text |
| `line_space` | <ApiLink name="int32_t" /> | line space of the text |
| `max_width` | <ApiLink name="int32_t" /> | max width of the text (break the lines to fit this size). Set COORD_MAX to avoid |
| `flag` | <ApiLink name="lv_text_flag_t" /> | settings for the text from <ApiLink name="lv_text_flag_t" /> |


</ApiMember>

## Enums

<ApiMember kind="enum" name="lv_text_flag_t" file="misc/lv_text.h" line={33} url="https://github.com/lvgl/lvgl/blob/master/src/misc/lv_text.h#L33">

### lv_text_flag_t

Options for text rendering.

| Name | Value | Description |
|------|------|------|
| `LV_TEXT_FLAG_NONE` | `0x00` |  |
| `LV_TEXT_FLAG_EXPAND` | `0x01` |  |
| `LV_TEXT_FLAG_FIT` | `0x02` | Max-width is already equal to the longest line. (Used to skip some calculation) |
| `LV_TEXT_FLAG_BREAK_ALL` | `0x04` | To prevent overflow, insert breaks between any two characters. Otherwise breaks are inserted at word boundaries, as configured via LV_TXT_BREAK_CHARS or according to LV_TXT_LINE_BREAK_LONG_LEN, LV_TXT_LINE_BREAK_LONG_PRE_MIN_LEN, and LV_TXT_LINE_BREAK_LONG_POST_MIN_LEN. |
| `LV_TEXT_FLAG_RECOLOR` | `0x08` | Enable parsing of recolor command |


</ApiMember>

<TypeUsedBy name="lv_text_flag_t" count={1}>

- `lv_text_get_size` — param `flag`

</TypeUsedBy>

<ApiMember kind="enum" name="lv_text_align_t" file="misc/lv_text.h" line={54} url="https://github.com/lvgl/lvgl/blob/master/src/misc/lv_text.h#L54">

### lv_text_align_t

Label align policy

| Name | Description |
|------|------|
| `LV_TEXT_ALIGN_AUTO` | Align text auto |
| `LV_TEXT_ALIGN_LEFT` | Align text to left |
| `LV_TEXT_ALIGN_CENTER` | Align text to center |
| `LV_TEXT_ALIGN_RIGHT` | Align text to right |


</ApiMember>

<TypeUsedBy name="lv_text_align_t" count={5}>

- `lv_obj_set_style_text_align` — param `value`
- `lv_bidi_calculate_align` — param `align`
- `lv_style_set_text_align` — param `value`
- `lv_spangroup_set_align` — param `align`
- `lv_textarea_set_align` — param `align`

</TypeUsedBy>

## Dependencies

<FileIncludes
  includes={["lv_conf_internal.h", "lv_types.h", "lv_area.h", "lv_font.h", "lv_sprintf.h"]}
  includedBy={["lv_draw.h", "lv_draw_label.h", "lv_bidi.h", "lv_style.h", "lv_text_ap.h", "lv_text_private.h", "lv_label.h"]}
  transitiveIncludes={["lv_assert.h", "lv_color.h", "lv_color_op.h", "lv_conf_kconfig.h", "lv_draw_buf.h", "lv_image_dsc.h", "lv_log.h", "lv_math.h", "lv_mem.h", "lv_palette.h", "lv_string.h", "lv_symbol_def.h"]}
/>